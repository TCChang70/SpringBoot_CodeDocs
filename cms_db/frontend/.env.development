# 開發環境變數（僅 npm run dev / vite 開發模式會讀取 .env.development）
# 後端所有端點名副其實地掛在 /api/v1 下；補上這個前綴，
# 前端就能以相對路徑（/auth/login、/categories...）打回正確位置，
# 並交由 vite.config.js 的 proxy 轉送給 http://localhost:8080。
VITE_API_BASE=/api/v1

# 媒體檔案網址（後端回傳 /media/files/{id} 已是根路徑，開發靠 proxy 轉送，不需設定）

# 正式部署：請在 build 環境設定 VITE_API_BASE 指向後端伺服器根源，
# 例如 VITE_API_BASE=http://api.example.com/api/v1