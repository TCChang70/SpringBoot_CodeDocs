server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_db_dev?useSSL=false&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true&characterEncoding=utf8mb4
    username: root
    password: 1234

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

logging:
  level:
    com.example: DEBUG
    org.hibernate.SQL: DEBUG
